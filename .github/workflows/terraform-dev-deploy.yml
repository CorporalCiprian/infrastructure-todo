name: Apply

on:
  push:
    branches: [main]

jobs:
  terraform_plan:
    uses: CorporalCiprian/todo-template/.github/workflows/terraform-deploy-template.yml@main
    secrets:
      AZURE_CREDENTIALS: ${{ secrets.AZURE_CREDENTIALS }}
    with:
      env: "dev"